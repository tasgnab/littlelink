# Database
DATABASE_URL=postgresql://username:password@host/database

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-here-generate-with-openssl-rand-base64-32

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
ALLOWED_USER_EMAIL=your-email@example.com

# Rate Limiting (Optional - defaults shown)
# API routes rate limit
RATE_LIMIT_API_REQUESTS=100
RATE_LIMIT_API_WINDOW_MS=60000

# Redirect routes rate limit (higher for primary function)
RATE_LIMIT_REDIRECT_REQUESTS=300
RATE_LIMIT_REDIRECT_WINDOW_MS=60000

# Auth routes rate limit (prevent brute force)
RATE_LIMIT_AUTH_REQUESTS=10
RATE_LIMIT_AUTH_WINDOW_MS=900000

# Strict rate limit for sensitive operations (API keys, etc.)
RATE_LIMIT_STRICT_REQUESTS=5
RATE_LIMIT_STRICT_WINDOW_MS=60000

# MaxMind GeoLite2 Geolocation (Optional)
# Get your free license key at https://www.maxmind.com/en/geolite2/signup
#
# For local storage (development/self-hosted):
# 1. Set MAXMIND_LICENSE_KEY
# 2. Run: npm run download-geodb

# MAXMIND_STORAGE_MODE=local|blob
MAXMIND_LICENSE_KEY=your-maxmind-license-key
MAXMIND_DATABASE_PATH=./data/GeoLite2-City.mmdb
MAXMIND_STORAGE_MODE=local
#
# For Vercel Blob storage (serverless deployments):
# 1. Set MAXMIND_LICENSE_KEY
# 2. Create a Vercel Blob store in your dashboard
# 3. Set BLOB_READ_WRITE_TOKEN from Vercel
# 4. Run: npm run upload-geodb-to-blob
# 5. In Vercel dashboard, set MAXMIND_STORAGE_MODE=blob
BLOB_READ_WRITE_TOKEN=vercel_blob_token_here
NEXT_PUBLIC_GRAVATAR=https://gravatar.com/avatar/your-gravatar-hash?v=timestamp&size=256
NEXT_PUBLIC_TITLE=Your App Title
NEXT_PUBLIC_TITLE_TAGLINE=Your App Tagline